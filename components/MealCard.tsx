'use client';
import { useState } from 'react'; import type { Meal } from '@/lib/types';
export function MealCard({meal}:{meal:Meal}){const[status,setStatus]=useState(meal.status||'previsto');async function update(next:'mangiato'|'saltato'){setStatus(next);await fetch('/api/diet/meal-status',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({plannedMealId:meal.id,status:next})})}return <article className="glass-card rounded-[1.6rem] p-4 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[.18em] text-cyan-600">{meal.mealType}</p><h3 className="mt-1 text-lg font-black">{meal.name}</h3><p className="text-sm text-slate-600">{meal.quantity} {meal.unit}</p></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-black">{status}</span></div><div className="mt-3 grid grid-cols-4 gap-2 text-center text-xs"><div className="rounded-2xl bg-slate-100 p-2"><b>{meal.calories}</b><br/>kcal</div><div className="rounded-2xl bg-slate-100 p-2"><b>{meal.proteinG}</b><br/>pro</div><div className="rounded-2xl bg-slate-100 p-2"><b>{meal.carbsG}</b><br/>carb</div><div className="rounded-2xl bg-slate-100 p-2"><b>{meal.fatG}</b><br/>fat</div></div><div className="mt-4 grid grid-cols-2 gap-2"><button onClick={()=>update('mangiato')} className="min-h-11 rounded-2xl bg-emerald-500 font-black text-white">Mangiato</button><button onClick={()=>update('saltato')} className="min-h-11 rounded-2xl bg-slate-950 font-black text-white">Saltato</button></div></article>}