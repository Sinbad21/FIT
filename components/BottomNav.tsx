'use client';
import Link from 'next/link'; import { usePathname } from 'next/navigation'; import { Dumbbell, History, Home, Salad, UserRound } from 'lucide-react';
const items=[['/','Oggi',Home],['/workout','Workout',Dumbbell],['/diet','Dieta',Salad],['/progress','Progressi',History],['/profile','Profilo',UserRound]] as const;
export function BottomNav(){const p=usePathname();return <nav className="fixed inset-x-0 bottom-0 z-50 mx-auto max-w-3xl px-3 pb-[calc(.75rem+env(safe-area-inset-bottom))] lg:max-w-5xl"><div className="grid grid-cols-5 rounded-[1.75rem] border border-gray-200/70 bg-white/95 p-2 shadow-soft backdrop-blur-xl">{items.map(([href,label,Icon])=>{const active=p===href||(href!=='/'&&p.startsWith(href));return <Link key={href} href={href} className={`flex min-h-14 flex-col items-center justify-center rounded-2xl text-[.72rem] font-bold transition ${active?'bg-gray-950 text-white':'text-gray-500 hover:bg-gray-100'}`}><Icon className="mb-1 h-5 w-5"/>{label}</Link>})}</div></nav>}